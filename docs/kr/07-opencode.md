# 7. OpenCode 설치 및 사용법

[← 목차로 돌아가기](../../README-KR.md)

---

OpenCode는 다양한 AI 모델을 지원하는 오픈소스 AI 코딩 도구입니다.

---

## 설치

### Windows

```
┌─────────────────────────────────────────────────────────────┐
│  ⚠️ Windows 사용자 안내                                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  OpenCode는 현재 Windows 네이티브 바이너리를 제공하지         │
│  않습니다. Windows 사용자는 아래 방법 중 선택하세요:          │
│                                                             │
│  ✅ 권장: Claude Code 사용 (Windows 완벽 지원)               │
│  🔧 대안: WSL(Linux 환경)에서 OpenCode 설치                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**방법 1: Claude Code 사용 (권장)** ⭐

Windows 사용자는 **Claude Code**를 사용하는 것을 강력 권장합니다.
→ [Claude Code 설치 가이드](06-claude-code.md)로 이동

**방법 2: WSL(Windows Subsystem for Linux)에서 설치**

WSL을 통해 Linux 환경에서 OpenCode를 사용할 수 있습니다.

1. **WSL 설치** (PowerShell 관리자 권한):
```powershell
wsl --install
```

2. 컴퓨터 **재시작**

3. **Ubuntu 터미널** 열기 (시작 메뉴에서 "Ubuntu" 검색)

4. Ubuntu에서 아래 Mac 설치 방법을 따라 설치

> **참고**: WSL 사용이 익숙하지 않으시면 Claude Code만 사용하셔도 충분합니다!

---

### Mac

**방법 1: Homebrew 사용** ⭐ 추천

```bash
brew install opencode-ai/tap/opencode
```

**방법 2: Go로 설치**

```bash
brew install go
go install github.com/opencode-ai/opencode@latest
```

---

## LiteLLM 연동 설정

OpenCode가 LiteLLM을 통해 작동하도록 설정 파일을 만듭니다.

### Mac / WSL (Linux)

1. 터미널에서 아래 명령어 실행:

```bash
nano ~/.opencode.json
```

2. 아래 내용 입력 (**API 키와 URL 수정**):

```json
{
  "provider": "openai-compatible",
  "apiKey": "sk-여기에API키입력",
  "baseUrl": "http://여기에서버주소입력",
  "model": "claude-4-5-opus"
}
```

3. `Ctrl + X` → `Y` → `Enter`로 저장

---

## 설치 확인

```bash
opencode --version
```

버전 번호가 나오면 **성공!**

---

## 첫 실행 및 사용법

1. 터미널에서 작업할 폴더로 이동:

```bash
# Mac / WSL
cd ~/Documents
```

2. OpenCode 실행:

```bash
opencode
```

---

## 주요 명령어

| 명령어 | 설명 |
|--------|------|
| `opencode` | OpenCode 시작 |
| `opencode --help` | 도움말 보기 |
| `opencode --version` | 버전 확인 |
| `/help` | 실행 중 도움말 |
| `/clear` | 대화 초기화 |
| `Ctrl + C` | 실행 중단 |
| `/quit` 또는 `exit` | 종료 |

---

## 사용 예시

```
> 현재 폴더에 있는 파일 목록을 보여줘

> index.html 파일 내용을 읽어줘

> React로 간단한 버튼 컴포넌트 만들어줘
```

---

[← 이전: Claude Code 설치](06-claude-code.md) | [다음: Skills & Agents →](08-skills-agents.md)
